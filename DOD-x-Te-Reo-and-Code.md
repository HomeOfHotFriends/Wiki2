# DOD × Te-Reo-and-Code

---

## Shared Tupuna

### Data-Oriented Design

```
I didn’t know what a cut back was, and 90% of my successful dodges were due to the fact I
was on level one in combination with the tiny
```

*— esprade_doc*

```
or level off to flat.
```

*— untitled2*


*Path → Te-Reo-and-Code: [Data-Oriented Design](dod) → [Te Whariki Pumotu](te-whariki-pumotu)*

### Zero Set

```
﻿This is the entire world: #include <array> #include <string> namespace
Nāwhaianō_nāwhaianōrā_Ināwhaianō { enum zero_set { pūwāhi_kau, pūtake_aukati,
huinga_whakaputa, katinotoa, kauaeheahea, huinga_kōwhiringa }; struct Te_Whariki_Pumotu {
void whakawātea() { ngā_zero_set.fill(pūwāhi_kau); } void whakatakoto(unsigned int x,
unsigned int y, zero_set z) { ngā_zero_set[taupū(x, y)] = z; } zero_set pānui(unsigned int
x, unsigned int y) const { return n
```

*— indigenous_proposal*

```
Deserialize)] pub enum Signal { Active(BytePair), Suppressed(ZeroSet), } impl Signal { pub
fn active(d: u8, c: u8) -> Self { Signal::Active(BytePair { d, c }) } pub fn pos_zero() ->
Self { Signal::Suppressed(ZeroSet::Positive) } pub fn neg_zero() -> Self {
Signal::Suppressed(ZeroSet::Negative) } pub fn total_zero() -> Self {
Signal::Suppressed(ZeroSet::Tot
```

*— indigenous_proposal*


*Path → DOD: [Zero Set](zero-set) → [Data-Oriented Design](dod)*


*Path → Te-Reo-and-Code: [Zero Set](zero-set) → [Te Whariki Pumotu](te-whariki-pumotu)*

### Te Whariki Pumotu

```
ay> #include <string> namespace Nāwhaianō_nāwhaianōrā_Ināwhaianō { enum zero_set {
pūwāhi_kau, pūtake_aukati, huinga_whakaputa, katinotoa, kauaeheahea, huinga_kōwhiringa };
struct Te_Whariki_Pumotu { void whakawātea() { ngā_zero_set.fill(pūwāhi_kau); } void
whakatakoto(unsigned int x, unsigned int y, zero_set z) { ngā_zero_set[taupū(x, y)] = z; }
zero_set pānui(unsigned int x, unsigned int y) const { return ngā_zero_set[taupū(x, y)]; }
void tāpiri(const Te_Whariki_Pumotu& atu, int nekeneke_x, int nekeneke_y) { for
```

*— indigenous_proposal*

```
(zero_set) #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)] pub enum
ZeroSet { /// +0 — positive suppression Positive, /// -0 — negative suppression Negative,
/// T0 — total axis suppression Total, } /// A paired discrete/continuous byte channel
(bytepair) #[derive(Debug, Clone, Copy, PartialEq, Eq, Default, Serialize,
```

*— indigenous_proposal*


*Path → DOD: [Te Whariki Pumotu](te-whariki-pumotu) → [Data-Oriented Design](dod)*

### Simplicity of Parts

**The design philosophy can be summed up as: extreme depth of the whole achieved through
extreme simplicity of the parts.**

*— esprade_doc*

**Anyways, the point is, through LOTS of really 'simple parts', and designing and sticking to
some key 'soft rules' I had achieved a work of astonishing depth.**

*— untitled2*


*Path → DOD: [Simplicity of Parts](simplicity-of-parts) → [Data-Oriented Design](dod)*


*Path → Te-Reo-and-Code: [Simplicity of Parts](simplicity-of-parts) → [Data-Oriented Design](dod) → [Te Whariki Pumotu](te-whariki-pumotu)*

---

## Divergence: Determinism vs Mauri

| gloss | body |
|---|---|
| ``` response bepositioning games are designed to be essentially completely deterministic. ``` | response bepositioning games are designed to be essentially completely deterministic. |

*Determinism: esprade_doc / Mauri: ai_singularity_te_reo*


*Rhizo bridge: [Determinism](determinism) → [The Law](the-law) → [Mauri](mauri)*

---


---

*Cross-generated by [WaKa](../WaKa.py) from `DOD` × `Te-Reo-and-Code`. All paths are lateral. Edit freely.*
